@{
    ViewData["Title"] = "Revisa tu correo";
}
<div class="alert alert-info mt-3">
    Si la cuenta existe, se envió un enlace para restablecer la contraseña. (Dev: usa el enlace de abajo)
</div>
@if (ViewData["DevResetUrl"] != null)
{
    <div class="card p-3 mt-2">
        <div>Enlace de restablecimiento (solo desarrollo):</div>
        <a href="@ViewData["DevResetUrl"]">@ViewData["DevResetUrl"]</a>
    </div>
}

